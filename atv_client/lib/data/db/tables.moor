CREATE TABLE programs (
    programId INT NOT NULL,
    externalId TEXT NOT NULL,
    channelExternalId TEXT NOT NULL,
    isDeleted BOOLEAN,
    PRIMARY KEY (programId, channelExternalId)
);

CREATE TABLE configs (
    id INT NOT NULL PRIMARY KEY,
    moviesInfo TEXT,
    imageBasePath TEXT,
    updating BOOLEAN
);